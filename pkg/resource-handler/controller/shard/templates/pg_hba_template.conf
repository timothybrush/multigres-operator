# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# Generated by Multigres - Helping you Scale PostgreSQL
#
# This file controls: which hosts are allowed to connect, how clients
# are authenticated, which PostgreSQL user names they can use, which
# databases they can access. Records take one of these forms:
#
# local      DATABASE  USER  METHOD  [OPTIONS]
# host       DATABASE  USER  ADDRESS  METHOD  [OPTIONS]
# hostssl    DATABASE  USER  ADDRESS  METHOD  [OPTIONS]
# hostnossl  DATABASE  USER  ADDRESS  METHOD  [OPTIONS]

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only.
# All local connections require SCRAM. Multipooler's admin pool authenticates
# with the password referenced by POSTGRES_PASSWORD_FILE; per-user connections
# authenticate via SCRAM passthrough.
local   all             all             scram-sha-256

# Replication connections. Standbys authenticate via SCRAM using a passfile
# pointed to by primary_conninfo (passfile=...). TCP rules are required because
# standbys run in separate pods and connect from pod IPs, not loopback.
local   replication     all                                     scram-sha-256
host    replication     all             0.0.0.0/0               scram-sha-256
host    replication     all             ::0/0                   scram-sha-256

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# IPv4 external connections
host    all             all             10.0.0.0/8              scram-sha-256
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256
host    all             all             0.0.0.0/0               scram-sha-256

# IPv6 external connections
host    all             all             ::0/0                   scram-sha-256
